feel free to add games to 2025/2026 since only the first is filled!
the table and matches will update as you add games and the info will be stored accross runtimes

some data was created using google gemini
because of this some (or all) of the special characters in player's names are incorrect (difference between VS and tkinter i believe)
player and place names may also be incorrect

as per school agreement, no code was written with ai, with the help of ai or using ai as a debug tool

# noqa
^ makes ruff extension ignore errors

# type: ignore
^ makes pylance ignore type errors
^ is used in places where something is defined with a union operator | to allow for multiple type and 
    later something is done that can only use some of the types included in the union operator